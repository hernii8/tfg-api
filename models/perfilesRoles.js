'use strict';

module.exports = function(sequelize, DataTypes) {
  const PerfilesRoles = sequelize.define('PerfilesRoles', {
  });

  PerfilesRoles.associate = function (models) {
  };

  return PerfilesRoles;
};
